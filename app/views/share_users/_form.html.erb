<%= form_for(@share_user) do |f| %>
  <% if @share_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@share_user.errors.count, "error") %> prohibited this share_user from being saved:</h2>

      <ul>
      <% @share_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :driver %><br>
    <%= f.number_field :driver %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
